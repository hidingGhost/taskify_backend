services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: securepw
    volumes:
      - pgdata:/var/lib/postgresql
 
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
 
volumes:
  pgdata: